
(library
  (name       event_name)
  (libraries  fourever)
)

; Do that for each file of results
(rule
 (target results.ml)
 (deps (source_tree results))
 (action (with-stdout-to %{null}
  (run ocaml-crunch -m plain results -o %{target}))))
